/**
 * main - prints a statement using puts, and goes to a new line
 * Return: 0 always
 */
#include<stdio.h> /* for standard output with puts */
int main(void)
{
	/* Outputing a message and a new line */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
