#include <stdio.h>
/**
 * main - computes and prints the sum of all the
 **/
int main(void)
{
	int sum, i;

	for (i = 0; i < 1024; i++)
	{
		if (a % 3 == 0 || a % 5 == 0)
		{
			sum += i;
		}
	}
	printf("%d\n", sum);
	return (0);
}
